# Dataset train / validation / test splitting utility

def split_dataset(data_dir, train_ratio=0.8, val_ratio=0.1, test_ratio=0.1, seed=42):
    """Splits dataset directory into train, validation, and test subsets."""
    pass
